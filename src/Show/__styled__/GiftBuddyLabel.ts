import styled from 'styled-components'

const GiftBuddyLabel = styled.div`
  padding: ${(props) => props.theme.spaces.large};
  padding-bottom: ${(props) => props.theme.spaces.medium};
  text-align: center;
  font-size: 2rem;
`

export default GiftBuddyLabel
